setTimeout(
    function corrida() {
        console.log('Inicia corrida') // função qualquer para inicio da corrida;
    }, 3000 // Aqui definimos o tempo que será contado antes que corrida() seja iniciado
)